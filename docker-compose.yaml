services:
  app:
    build: .
    container_name: ficky_be 
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "3007:${APP_PORT}"
    networks:
      - postgres_network

networks:
  postgres_network:
    external: true
